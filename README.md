# My packages

```
sudo pacman -S discord docker docker-compose wireshark-qt obs-studio firefox wmctrl evolution 
```

```
pacman -S --needed git base-devel
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
```

```
cd .. && sudo rm -r yay
```

```
yay -S spotify visual-studio-code-bin jetbrains-toolbox surfshark-gui-bin postman-bin arduino-ide-bin ulauncher slack-desktop notion-app mqtt-explorer balena-etcher rpi-imager zerotier-one brave-bin google-chrome
```
